# ashares_data_collect 新闻系统静态审计报告

审计日期：2026-08-15  
审计方式：只读静态代码审计；未安装依赖、未运行爬虫、未访问生产数据库/OpenSearch、未执行测试。  
审计范围：项目全量结构与配置，重点审读 `run_news.py`、`sources.yaml`、`config.example.yaml`、`run_job.py`、`data_collect/pipeline.py`、`data_collect/jobs/news_*.py`、全部新闻相关 `utils`、OpenSearch/搜索/归档/正文抽取/股票与行业映射代码，以及相关测试目录的能力覆盖。

> 重要边界：仓库中没有 `config.yaml`，只有 `config.example.yaml`。因此本报告能确认“代码实现了什么、模板声明调度什么”，不能确认生产机是否已配置 `rsshub_base`、`us_proxy`、OpenSearch、PostgreSQL、NAS、向量模型，也不能确认任何源在审计当日实际在线、实际有数据或数据量完整。

## 一、结论摘要

当前系统已经形成了较扎实的新闻采集与检索底座：独立调度入口、多源采集、源级隔离、硬超时、原始归档、spool 降级、自然日 verify、确定性 `_id`、OpenSearch 按年索引、BM25/kNN/hybrid 检索、个股/公告全文回填和向量补齐都已实现。

但系统目前更准确的定位是：

> **“多源新闻采集、归档、检索平台”，还不是“全市场重要新闻雷达”，也还不是“候选股自动深挖引擎”。**

两个目标的当前状态：

| 目标 | 结论 | 核心原因 |
|---|---|---|
| 全市场新闻雷达 | **部分支持，未完成** | 有持续采集和粗分类，但关键媒体/交易所/部委覆盖不全；公司公告和个股新闻仅每日一次；没有重要度、情绪、事件聚类、跨源近重复去重和面向用户的雷达排序/告警 |
| 候选股新闻深挖 | **基础检索可用，自动深挖未完成** | 可以按规范股票代码过滤 `stocks[]`，也有个股新闻、公告、研报和语义检索；但没有输入解析、公司别名消歧、行业/产业链扩展、海外事件关联、研报结构化聚合和统一深挖编排 |

一句话判断：**采集层和存储检索层值得保留；真正缺失的是新闻智能层、关系层和覆盖完整性。**

## 二、现有系统实际架构

```text
sources.yaml / config.yaml
        │
        ├─ RSS / Atom / RSSHub
        ├─ 官方 API / AkShare 封装
        └─ 列表页、正文页、PDF 网页抓取
        │
        ▼
news_* jobs
        │  统一信封、文本清洗、时间归一、stocks[] 初步打标
        ├───────────────┐
        ▼               ▼
NAS JSONL.gz 归档       OpenSearch news-{year}
spool 降级 + verify      create-only 幂等写入
                        │
                        ├─ news_fulltext / news_announce_pdf
                        ├─ news_embed
                        └─ search_news(BM25/kNN/hybrid/RRF)

PostgreSQL instrument_info ──► 股票简称→代码打标
PostgreSQL sector_stock ──────X 目前未接入新闻处理或检索
```

### 调度能力

`run_news.py` 只负责收窄新闻入口，实际调度复用 `run_job.py` 和 `data_collect.pipeline`。模板中声明 5 条新闻 pipeline：

| Pipeline | 模板频率 | 任务 |
|---|---:|---|
| `news_flash` | 每 15 分钟 | 财联社/东财/可选新浪快讯 |
| `news_hourly` | 每小时 `:37` | 政策 → 监管/媒体/研报 → 个股全文 → 公告 PDF → 向量 |
| `news_daily` | 每日 20:40 | 联播、各类 verify、全市场公告、日报 |
| `news_stock` | 每日 22:00 | 全市场逐票个股新闻 + 归档重放 |
| `news_us` | 每日 05:30 | 美国官方/媒体 RSS + verify |

任务由独立子进程执行，具有任务级超时、重试、依赖失败跳过和钉钉通知。该框架层可继续保留。

## 三、当前新闻数据源清单

### 3.1 数量与配置事实

`sources.yaml` 共登记 **47 个源**：

- 41 个 `enabled=true`（含默认启用）；
- 6 个 `enabled=false`：`stats`、`bbc_world`、`bbc_business`、`bbc_tech`、`npr_news`、`aljazeera`；
- 适配器分布：18 个 RSS/Atom、23 个 RSSHub、3 个列表页抓取、2 个 AkShare、1 个官方 API；
- 另有未进入注册表的硬编码/mini-registry 源：快讯 `cls`、`em`、可选 `sina`，以及东财财经早餐 `em_cjzc`；
- 央视官网是 `cctv` 的备源，央视 RSSHub 路由只做交叉核验，不作为主入库源。

按代码默认值推算，若部署配置完整，默认会尝试 41 个注册源 + `em_cjzc` + 快讯 `cls/em`，即 **44 个采集源 ID**；`sina` 只有写入 `news.flash_sources` 后才启用。这里的“尝试”不代表本次审计验证过在线状态。

### 3.2 快讯、联播、公告、个股

| 源/路径 | 状态 | 类型 | 抓取内容 | 通道/频率 |
|---|---|---|---|---|
| `cls` | 默认启用，未进 `sources.yaml` | 财联社官方 JSON API | 财联社滚动电报，单窗最多 50 条 | `flash`，15 分钟 |
| `em` | 默认启用，未进 `sources.yaml` | AkShare/东财接口 | 东财全球财经快讯，滚动窗约 200 条 | `flash`，15 分钟 |
| `sina` | 可选，默认不启用 | AkShare/新浪接口 | 新浪全球财经快讯，滚动窗约 20 条 | `flash`，配置启用后 15 分钟 |
| `cctv` | 启用 | AkShare | 指定自然日的《新闻联播》分条文字稿 | `cctv`，每日；可补历史至 2016-02-03 |
| `tv.cctv.com` | 主源空/失败时启用 | 网页抓取 | 央视联播日页和分条正文 | 与 `cctv` 共用 source/id 语义 |
| RSSHub `/cctv/tv/lm/xwlb` | 有 `rsshub_base` 时核验 | RSSHub，仅审计 | 联播每期摘要分条数，与库内条数交叉核验 | 不直接入库 |
| `cninfo` | 启用 | 巨潮官方 JSON API | 深沪京 A 股当日全市场公司公告元数据；随后下载 PDF 抽正文 | `announcement`，每日 20:40 |
| `em_stock` | 启用 | AkShare/东财接口 | 遍历全部 A 股，每票最新 10 条个股新闻；按 URL 聚合跨票关系 | `stock`，每日 22:00 |
| 个股新闻原文 URL | `body_status=pending` 时处理 | 网页抓取 | requests/curl_cffi 获取页面，trafilatura 抽正文 | 每小时回填 |
| 巨潮 PDF URL | `pdf_status=pending` 时处理 | PDF 下载/文本层抽取 | 下载不超过 20MB 的 PDF，PyMuPDF 抽文本；扫描件不 OCR | 每小时回填 |

### 3.3 国内政策、监管、媒体和研报

| 源 ID | 状态 | 类型 | 抓取内容 | channel |
|---|---|---|---|---|
| `govcn_policy` | 启用 | 官方 RSS | 中国政府网最新政策 | `policy` |
| `govcn_gwy` | 启用 | 官方 RSS | 中国政府网国务院动态/文件 | `policy` |
| `stats` | **停用** | 官方 RSS | 国家统计局最新发布；注释说明因源头挂死停用 | `policy` |
| `em_cjzc` | 启用，未进注册表 | AkShare | 东财财经早餐：标题、摘要、发布时间、链接 | `media` |
| `ndrc` | 启用但依赖 RSSHub | RSSHub | 国家发改委新闻发布 | `policy` |
| `gov_bmwj` | 启用但依赖 RSSHub | RSSHub | 中国政府网政策文件库中的部委文件 | `policy` |
| `szse_notice` | 启用但依赖 RSSHub | RSSHub | 深交所公告/通知 | `policy` |
| `csrc` | 启用 | 列表页 + 正文网页 | 证监会列表页文章，抓前按 URL 去重，抓正文 | `policy` |
| `mof` | 启用 | 列表页 + 正文网页 | 财政部财政新闻 | `policy` |
| `people` | 启用 | 列表页 + 正文网页 | 人民网财经文章 | `media` |
| `cls_red` | 启用但依赖 RSSHub | RSSHub | 财联社加红电报 | `flash` |
| `gelonghui` | 启用但依赖 RSSHub | RSSHub | 格隆汇快讯 | `flash` |
| `cls_depth` | 启用但依赖 RSSHub | RSSHub | 财联社深度头条 | `media` |
| `caixin` | 启用但依赖 RSSHub | RSSHub | 财新最新 | `media` |
| `thepaper` | 启用但依赖 RSSHub | RSSHub | 澎湃首页/精选 | `media` |
| `guancha` | 启用但依赖 RSSHub | RSSHub | 观察者网头条 | `media` |
| `ckxx` | 启用但依赖 RSSHub | RSSHub | 参考消息中国栏目 | `media` |
| `cctv_world` | 启用但依赖 RSSHub | RSSHub | 央视国际新闻 | `media` |
| `wallstreetcn_news` | 启用但依赖 RSSHub | RSSHub | 华尔街见闻资讯 | `media` |
| `wallstreetcn_live` | 启用但依赖 RSSHub | RSSHub | 华尔街见闻快讯 | `media` |
| `yicai` | 启用但依赖 RSSHub | RSSHub | 第一财经头条 | `media` |
| `jiemian` | 启用但依赖 RSSHub | RSSHub | 界面要闻 | `media` |
| `kr36_flash` | 启用但依赖 RSSHub | RSSHub | 36氪快讯 | `media` |
| `kr36_hot` | 启用但依赖 RSSHub | RSSHub | 36氪热榜 | `media` |
| `latepost` | 启用但依赖 RSSHub | RSSHub | 晚点文章 | `media` |
| `huxiu` | 启用但依赖 RSSHub | RSSHub | 虎嗅文章 | `media` |
| `em_report_strategy` | 启用但依赖 RSSHub | RSSHub | 东财策略研报 | `report` |
| `em_report_industry` | 启用但依赖 RSSHub | RSSHub | 东财行业研报 | `report` |
| `em_report_macro` | 启用但依赖 RSSHub | RSSHub | 东财宏观研报 | `report` |
| `em_report_stock` | 启用但依赖 RSSHub | RSSHub | 东财个股研报 | `report` |

上述 23 个 RSSHub 源只有在生产 `config.yaml` 配置 `news.rsshub_base` 时才能拉取。仓库模板里该配置是注释项，本次无法确认生产是否已配置。

### 3.4 美国和国际新闻

以下源由 `news_us` 每日 05:30 拉取。代码支持按源使用 `news.us_proxy`，但仓库没有生产配置，不能确认代理实际存在。

| 源 ID | 状态 | 类型 | 抓取内容 | channel |
|---|---|---|---|---|
| `fed` | 启用 | 美联储官方 RSS | 美联储新闻稿、FOMC、讲话 | `us_policy` |
| `sec_8k` | 启用 | SEC EDGAR Atom | 当前滚动窗内 8-K 重大事件报告，最多 400 条；不是全量 daily-index | `us_filing` |
| `nyt_business` | 启用 | RSS | 纽约时报商业 | `us_news` |
| `nyt_economy` | 启用 | RSS | 纽约时报经济 | `us_news` |
| `wsj_markets` | 启用 | RSS | 华尔街日报市场 | `us_news` |
| `wsj_business` | 启用 | RSS | 华尔街日报商业 | `us_news` |
| `wsj_world` | 启用 | RSS | 华尔街日报世界 | `us_news` |
| `bloomberg_markets` | 启用 | RSS | 彭博市场标题/摘要 | `us_news` |
| `guardian_business` | 启用 | RSS | 卫报商业，feed 可提供较完整正文 | `us_news` |
| `marketwatch` | 启用 | RSS | MarketWatch 头条 | `us_news` |
| `bbc_world` | **停用候选** | RSS | BBC 世界 | `intl_news` |
| `bbc_business` | **停用候选** | RSS | BBC 商业 | `intl_news` |
| `bbc_tech` | **停用候选** | RSS | BBC 科技 | `intl_news` |
| `npr_news` | **停用候选** | RSS | NPR 新闻 | `intl_news` |
| `aljazeera` | **停用候选** | RSS | 半岛电视台全球新闻 | `intl_news` |

### 3.5 按采集方式归类

| 类型 | 当前实现 |
|---|---|
| 官方/公开 API | 财联社 v1、巨潮公告 API；AkShare 封装的东财/新浪/央视/个股新闻/财经早餐也属于接口型采集 |
| RSS/Atom | 中国政府网、国家统计局、Fed、SEC Atom、NYT、WSJ、Bloomberg、Guardian、MarketWatch，以及 5 个停用国际候选 |
| RSSHub | 发改委、部委文件库、深交所、财联社/格隆汇、国内媒体、东财研报等 23 路；央视联播另有 1 路仅核验 |
| 网页抓取 | 证监会、财政部、人民网列表页+正文；央视官网备源；个股新闻原文全文抓取 |
| PDF | 巨潮公告 PDF 下载和文本层抽取；无 OCR |

## 四、对 15 个问题的逐项回答

| # | 问题 | 审计结论 |
|---:|---|---|
| 1 | 当前有哪些新闻数据源 | 见第三章。注册表 47 个源，另有快讯/财经早餐等注册表外源，以及央视官网备源/核验源 |
| 2 | 每个源抓什么 | 已按快讯、联播、政策、监管媒体、研报、公告、个股、美国/国际逐源列出 |
| 3 | 哪些是 API、RSS/RSSHub、网页抓取 | 已明确分类；五类采集方式均存在 |
| 4 | 是否支持全市场新闻持续抓取 | **部分支持。** 快讯 15 分钟、政策/监管小时级，具备持续调度；但公告、个股新闻仅日级，关键官方/媒体源缺口明显，也没有“重要新闻雷达”的排序和推送层，不能认定目标已实现 |
| 5 | 是否支持按单只股票搜索相关新闻 | **基础支持。** `search_news(..., stocks="600519.SH")` 可按规范代码过滤；个股新闻和公告的 `stocks[]` 较可靠。但没有“输入任意股票→自动扩展公司/行业/政策/上下游/海外/研报”的统一入口，因此深挖目标未实现 |
| 6 | 是否有新闻去重 | **有确定性/精确去重，没有事件级近重复去重。** `_id` 使用 native id、URL 或 source+time+title；批内去重、归档 ID 去重、OpenSearch create-only 都已实现；可选 `collapse=title.raw` 只能去精确同题。不同 URL、不同标题描述同一事件仍会重复 |
| 7 | 是否有股票映射 | **部分有。** `instrument_info` 提供当前简称→规范代码词典，正文用代码正则和简称子串打 `stocks[]`；公告使用权威 `secCode`；个股新闻使用被扫描代码。缺少别名、曾用名、全称、品牌、子公司、人物、歧义消解、历史版本和可重跑的实体重标注 |
| 8 | 是否有行业映射 | **底层数据有，新闻系统内没有。** `a_share_sector.py` 维护 GICS/同花顺行业、概念、风格到股票的 `sector_stock`；另有“股票全部信息汇总”的所属行业，但新闻入库、OpenSearch mapping、检索函数均未使用这些数据 |
| 9 | 是否有重要度评分 | **没有。** OpenSearch `_score`/RRF 分数是针对用户查询的相关性，不是新闻的重要度、影响力或市场冲击分数 |
| 10 | 是否有利好/利空/中性判断 | **没有。** 仓库新闻代码中没有 sentiment/polarity/利好利空字段、模型或规则 |
| 11 | 是否有事件聚类 | **没有。** 没有 `event_id`、聚类任务、时间窗聚合、事件主稿/跟进稿关系或事件热度 |
| 12 | 是否覆盖上证报、证券时报、中国证券报、财联社 | **只明确覆盖财联社。** 财联社有官方快讯 API、RSSHub 加红电报和深度头条；未发现上海证券报、证券时报、中国证券报的独立源。其他平台偶然转载不等于稳定覆盖 |
| 13 | 是否覆盖上交所、深交所、北交所、证监会和主要部委 | **部分。** 深交所有 RSSHub 通知；证监会有官网列表页；巨潮公司公告覆盖深沪京公司披露。没有上交所和北交所的直接交易所源；主要部委仅政府网/部委文件库、发改委、财政部，统计局源还处于停用；央行、工信部、商务部、国资委、市场监管总局等没有独立直接源 |
| 14 | 离两个目标还缺什么 | 缺源覆盖、实体/行业/产业链关系、重要度、情绪、事件聚类、近重复合并、候选股深挖编排、海外映射、研报结构化、实时性与完整性监控，详见第七章 |
| 15 | 哪些保留，哪些二开 | 调度、归档、规范化、OpenSearch、官方 API 和全文/向量基础应保留；实体关系、评分聚类、源覆盖、候选股编排、查询结果和数据治理需要二次开发，详见第八章 |

## 五、关键能力的真实边界

### 5.1 全市场持续抓取：有调度，不等于已有雷达

已经具备：

- `run_news.py` 独立于行情系统，可单独部署；
- 15 分钟、小时、日级多频率调度；
- 源级失败隔离、硬超时、重试、断流/溢出告警；
- 快讯滚动窗归档、spool 降级和归档重放；
- 全市场公告单查询和全市场逐票个股新闻扫描。

仍不能称为完整雷达：

- 没有“只输出重要新闻”的重要度模型与排序；
- 没有事件合并，同一事件的多家快讯/报道会分别出现；
- 没有面向市场主题、行业和板块的统一分类树；
- 公司公告 20:40 一次、个股新闻 22:00 一次，不是持续发现；
- 快讯和个股源都是滚动窗，错过窗口后源头不可回补；
- 日报主要监控 channel 数量和向量积压，不是业务新闻雷达；
- RSSHub 23 路是单一基础设施依赖，配置缺失或服务异常会大幅缩小覆盖。

### 5.2 单股搜索：精确过滤可用，深挖流程不存在

现有 `data_collect.utils.news_search.search_news()` 支持：

- `stocks` 精确 terms 过滤；
- `channel` 和日期范围过滤；
- BM25、kNN、OpenSearch hybrid、客户端 RRF；
- 查询文本繁简/全半角清洗；
- 默认排除系统 marker。

但它仍是通用检索函数，不是候选股调查器：

- 调用方必须已经知道规范代码，例如 `600519.SH`；
- 没有股票名称/代码输入解析和候选消歧；
- 不会自动联查公司公告、公司新闻、行业、政策、上下游、海外事件和研报并分栏汇总；
- 不会读取 `sector_stock` 扩展同业或概念板块；
- 没有供应链图谱、产品/原材料/客户/供应商关系；
- 海外源明确以空 A 股词典入库，基本不会得到 A 股 `stocks[]`；
- 研报只是 `channel=report` 的 RSSHub feed，没有机构、分析师、评级、目标价、报告类型等结构化字段；
- 返回字段只有标题、来源、通道、时间、URL、分数和高亮，不返回 `stocks`、正文摘要、事件、行业、情绪、重要度或命中原因。

### 5.3 股票映射的覆盖与风险

当前映射链路：

1. `a_share_instrument.py` 从 QMT 建 `instrument_info` 当前快照；
2. `news_normalize.load_name_dict()` 读取 `InstrumentName`，生成 `{简称: 规范代码}`；
3. `tag_stocks()` 用 6 位代码正则和简称子串匹配；
4. 公告直接使用巨潮 `secCode`；
5. 个股新闻将“本次被查询的股票”与文本打标结果合并。

局限：

- 只有当前证券名称，没有简称历史、曾用名、品牌、产品、子公司和英文名；
- 简称采用直接子串匹配，没有词边界、歧义词和上下文消歧；
- 快讯只对 `title` 打标，未使用 `content`；新浪快讯没有标题，因此其 `stocks[]` 实际始终为空；
- 联播也只对标题打标，没有用全文；
- 政策/监管和个股新闻使用标题+内容，覆盖相对好；
- create-only 会让初次漏标的 `stocks[]` 保持不变，后续词典恢复或实体规则升级不会自动修正；
- 没有独立的实体标注版本和批量重标机制。

因此，`stocks[]` 适合做当前的粗过滤索引，不应当被视为完整、可审计的公司实体关系。

### 5.4 行业映射：数据存在但链路断开

`a_share_sector.py` 已采集：

- GICS 1~4 级；
- 同花顺 1~3 级行业；
- 同花顺概念；
- 同花顺风格。

数据落 PostgreSQL `sector_stock`/`sector_changelog`。此外 `wave3.py` 会读取“股票全部信息汇总”的 `所属行业`。

但新闻系统没有：

- `industries[]`、`concepts[]` 或行业层级字段；
- 新闻→股票→行业的入库 enrichment；
- 行业新闻反向映射到成分股；
- 按候选股行业扩展查询；
- 产业链上下游关系。

所以对“新闻系统是否有行业映射”的准确答案是：**有可复用的底表，没有新闻行业映射能力。**

### 5.5 去重：精确幂等较强，语义去重缺失

已实现的去重层次：

- native id：巨潮公告、RSS guid；
- URL SHA-1：个股文章、带链接 RSS；同 URL 跨源可天然合并；
- 无 URL 时使用 `source|pub_time|title`；
- 批内 `_id` 去重；
- 归档按 `_id` 过滤；
- OpenSearch `op_type=create`，409 计重复；
- 个股新闻先按 URL 聚合多个股票到同一文档；
- 搜索可选 `collapse="title.raw"` 做精确同题折叠。

缺失：

- 标题轻微改写的近重复去重；
- 不同 URL 的转载识别；
- 快讯、公告、媒体报道之间的同事件聚合；
- 事件主稿、跟进稿、解读稿关系；
- 跨语种同事件合并。

因此现有能力应称为“文档幂等和精确去重”，不是“新闻事件去重”。

### 5.6 OpenSearch 与搜索层

值得保留的实现：

- 物理索引 `news-{year}`，读别名 `news`；
- 自动探测 IK/smartcn/standard analyzer；
- `title.raw` 精确标题字段；
- `content_vec` 1024 维 HNSW；
- create-only 写入和显式 `(_index, _id)` 局部更新；
- BM25、kNN、hybrid、RRF 四种查询；
- 向量关闭时自动降级 BM25；
- 正文/PDF 回填后重新置 `vec_status=pending`，可重算全文向量。

需要注意的真实代码边界：

- `news_search._bm25_query()` 只搜索 `title^2` 和 `content`，**没有搜索 `body`**；个股网页全文和公告 PDF 全文只能通过向量间接参与语义检索。项目 skill 文档里的 BM25 示例包含 `body`，与实际代码不一致；
- `news_search._SOURCE_FIELDS` 不返回 `stocks`、`content`、`body`；
- `ann_type` 虽在 mapping 预留，但当前公告入库没有填充公告分类；
- `summary` mapping 基本未使用；
- 没有行业、事件、情绪、重要度、实体置信度、来源权重等字段；
- `body_status=failed` 和 `pdf_status=failed` 不会自动重试，瞬时失败也会永久停止处理；
- 文档主体 create-only 很稳健，但也使后续股票重标、行业映射和模型重算需要专门的 enrichment 更新协议或独立索引。

## 六、指定媒体和官方机构覆盖

### 6.1 四家财经媒体

| 媒体 | 直接覆盖 | 证据/判断 |
|---|---|---|
| 上海证券报（上证报） | **否** | `sources.yaml` 和新闻代码无独立源 |
| 证券时报 | **否** | 无独立源 |
| 中国证券报 | **否** | 无独立源 |
| 财联社 | **是** | 官方快讯 API `cls`，RSSHub `cls_red`、`cls_depth` |

### 6.2 交易所、证监会与部委

| 机构 | 直接覆盖 | 当前情况 |
|---|---|---|
| 上交所 | **否** | 没有上交所新闻、监管动态、规则、问询、纪律处分等直接源 |
| 深交所 | **部分** | RSSHub `/szse/notice`；依赖 RSSHub，不是代码直连官方接口 |
| 北交所 | **否** | 没有北交所直接源；巨潮只解决上市公司公告，不等于北交所监管/规则覆盖 |
| 证监会 | **是** | 官网列表页+正文抓取 |
| 国家发改委 | **是，条件性** | RSSHub 路由，依赖 `rsshub_base` |
| 财政部 | **是** | 官网列表页+正文抓取 |
| 国家统计局 | **登记但停用** | RSS 源因挂死被 `enabled=false` |
| 中国政府网/国务院/部委文件库 | **部分覆盖** | 两个官方 RSS + 一个 RSSHub 部委文件聚合 |
| 中国人民银行 | **无独立源** | 代码注释说明 PBC 因 JS 正文问题移除，仅期待其他渠道间接覆盖 |
| 工信部、商务部、国资委、市场监管总局等 | **无独立直接源** | 可能被政府网聚合偶尔覆盖，但不构成稳定、可核验的直接覆盖 |

## 七、距离两个目标还缺什么

### 7.1 目标一：全市场新闻雷达

按优先级看，主要缺口如下。

#### P0：决定“是不是雷达”的能力

1. **重要度评分**
   - 需要来源权威度、事件类型、涉及市值/股票数量、政策层级、公告类型、异常传播速度、市场交易时段、是否首次报道、价格/成交异动等特征；
   - 当前 OpenSearch score 只能表示“与查询有多相关”，不能替代重要度。

2. **事件聚类与跨源近重复去重**
   - 把快讯、媒体稿、公告、政策原文聚合为一个事件；
   - 保留首发、权威原文、跟进、解读和市场反应，而不是简单删除文档。

3. **更完整的权威源覆盖**
   - 三大证券报；
   - 上交所、北交所，强化深交所直连；
   - 央行、工信部、商务部、国资委、市场监管总局、能源局等；
   - 交易所问询、监管措施、纪律处分、规则变化等结构化类别。

4. **业务分类体系**
   - 当前只有粗 `channel`；
   - 至少需要宏观、货币、财政、产业政策、监管、交易所、公司事件、财报、融资、并购、回购、减持、诉讼、事故、供需、价格、技术、地缘、机构观点等事件类型。

5. **面向用户的雷达输出**
   - 当前只有库和健康日报；
   - 需要按时间窗输出去重事件流、重要度排序、分类分栏、受影响股票/行业及可信来源，并支持阈值告警。

#### P1：完整性与可运营性

6. **提升时效**：公告和个股新闻从每日一次改为盘中/盘后增量；对关注池可更高频。  
7. **每源完整性监控**：日报应按 source 统计条数、最后发布时间、延迟、连续空窗、解析失败率，而不只是 channel 总量。  
8. **失败重处理**：正文/PDF 瞬时失败需要带次数和退避的重试队列，而不是一次失败永久 `failed`。  
9. **原文与版权边界**：区分可存全文、只存摘要/链接、付费墙来源，记录抓取许可和可用字段。  
10. **数据质量版本化**：实体标注、分类、情绪、评分、聚类应带模型/规则版本，支持重算。

### 7.2 目标二：候选股新闻深挖

需要新增一个“候选股调查编排层”，输入股票代码或名称后至少执行：

1. **股票身份解析**
   - 代码、简称、全称、曾用名、英文名、品牌、子公司、核心产品；
   - 对常见词简称做歧义消解。

2. **直接公司层**
   - `stocks[]` 精确过滤；
   - 公司公告、个股新闻、媒体新闻、快讯分别取回；
   - 公告按类型结构化，提取金额、日期、交易对手、业绩指标等。

3. **行业与板块层**
   - 利用现有 `sector_stock` 取得 GICS/同花顺行业、概念、风格；
   - 搜索行业新闻和政策，并说明为何与候选股相关。

4. **上下游产业链层**
   - 建立公司—产品—原材料—设备—客户—供应商—地区—竞争对手关系；
   - 当前项目没有任何可直接复用的产业链关系表。

5. **海外事件层**
   - 把海外公司、商品、利率、汇率、制裁、技术、地缘事件映射到 A 股公司/行业；
   - 当前海外新闻 `stocks=[]`，只能靠人工关键词/语义搜索，不能自动关联。

6. **研报/机构观点层**
   - 当前仅有 4 路东财研报 feed；
   - 需要机构、分析师、发布日期、评级变化、目标价、盈利预测、报告类型、覆盖标的、核心观点和原文权限字段。

7. **统一结果排序与解释**
   - 结合相关性、重要度、新鲜度、来源权威、事件状态、情绪和关系距离排序；
   - 返回“为什么与这只股票相关”，而不是只给 OpenSearch score。

## 八、模块保留与二次开发建议

### 8.1 建议直接保留的模块

| 模块 | 建议 | 原因 |
|---|---|---|
| `run_news.py` + `run_job.py` 新闻边界 | 保留 | 入口职责分离清晰 |
| `data_collect.pipeline` | 保留 | 子进程隔离、超时、重试、DAG 和通知是可靠底座 |
| `source_registry.py` / `source_adapters.py` | 保留 | 源配置集中、kill-switch、last-good、HTTP timeout 设计成熟 |
| `news_archive.py` | 保留 | 原始信封归档、spool 降级、重放和自然日路径设计有价值 |
| `news_common.py` | 保留 | 日期、超时、raw 字段、verify 窗口等公共契约清晰 |
| `news_normalize.py` 的清洗、时间、ID 逻辑 | 保留 | 决定性和幂等性较好 |
| `opensearch_utils.py` | 保留 | 按年索引、别名、analyzer 探测、create-only、显式更新合理 |
| `news_search.py` 的四种检索框架 | 保留 | BM25/kNN/hybrid/RRF 可作为后续检索底座 |
| `bodyfill.py`、`fulltext.py`、`pdftext.py` | 保留 | 全文/PDF 抽取的共享状态机可复用 |
| `cninfo.py`、财联社直连 API | 保留 | 官方/一手接口价值高 |
| `news_flash` 的断流/溢出监控 | 保留 | 对滚动窗源很必要 |
| `a_share_instrument.py`、`a_share_sector.py` | 保留 | 是实体和行业 enrichment 的基础数据源 |

### 8.2 建议保留但重点二开的模块

| 模块 | 二次开发重点 |
|---|---|
| `sources.yaml` | 补三大证券报、上交所、北交所、央行及主要部委；为源增加权威等级、地区、语言、内容许可、预期频率和健康阈值；减少注册表外硬编码源 |
| `news_flash.py` | 对标题为空的源使用正文打股票标签；建立快讯事件聚类和首发识别 |
| `news_policy.py` / `news_regulator.py` | 增加政策层级、发布机关、文号、主题、适用行业；RSS 摘要源增加正文回填策略 |
| `news_announcement.py` | 填充 `ann_type`，做公告类型和关键字段抽取；提高增量采集频率 |
| `news_stock.py` | 保留全市场扫作为兜底，新增候选池高频采集、多来源交叉验证和后续 stocks 重标能力 |
| `news_us.py` | 增加亚洲/商品/科技/地缘等一手源，并建立海外实体/主题到 A 股行业和公司的映射 |
| `news_fulltext.py` / `news_announce_pdf.py` | 增加可控重试、失败原因、OCR 队列和来源级策略 |
| `news_normalize.py` | 从简称子串升级为实体解析器；增加别名、历史名称、子公司、品牌、置信度和标注版本 |
| `news_search.py` | BM25 纳入 `body`；支持行业、事件、情绪、重要度过滤；返回 stocks/行业/事件/摘要/命中理由；新增候选股深挖编排接口 |
| `opensearch_utils.py` | 扩展 mapping：`event_id`、`event_type`、`industries`、`concepts`、`entities`、`importance`、`sentiment`、`relation_reason`、模型版本等 |
| `news_report.py` | 从 channel 总量升级为 source 级健康、延迟、失败率、正文成功率、未聚类量和高重要度事件摘要 |
| `a_share_sector.py` | 增加查询/服务层，把行业映射真正接到新闻 enrichment 与候选股搜索 |

### 8.3 不建议推倒重写的部分

不建议重写调度、原始归档、OpenSearch 基础设施、确定性 ID 和全文/向量回填框架。这些模块已经解决了大量生产级边界问题。更合适的路线是在其上增加：

- 独立 enrichment pipeline；
- 实体/行业/产业链关系层；
- 事件表或事件索引；
- 评分、情绪与聚类字段；
- 候选股调查编排和雷达输出。

文档正文可继续 create-only，智能标注建议采用可版本化的独立 enrichment 文档或受控局部更新，避免现有“首次漏标永久保留”的限制。

## 九、建议的下一阶段建设顺序

本报告只审计、不实施。若下一阶段开始开发，建议顺序为：

1. **定义统一新闻/事件/实体数据模型**，先明确字段、版本和更新语义；
2. **补权威源覆盖**：三大证券报、上交所、北交所、央行及主要部委；
3. **接通股票与行业 enrichment**：复用 `instrument_info`、`sector_stock`，建立可重标机制；
4. **实现候选股深挖编排**：直接新闻/公告 → 行业政策 → 产业链 → 海外 → 研报；
5. **实现近重复和事件聚类**；
6. **实现重要度、情绪和影响对象判断**；
7. **升级搜索返回与雷达排序/告警**；
8. **补 source 级完整性监控、重试队列和离线评测集**。

## 十、审计中发现的文档/实现漂移

这些不等于代码故障，但会误导后续开发和运维：

1. `README.md` 称 `news_regulator` 有 27 个源；当前 `sources.yaml` 实际为 23 个 RSSHub + 3 个 listpage，共 26 个。  
2. `news_policy.py` 顶部称官方 RSS×3，但 `stats` 已停用，实际默认是 2 个官方 RSS + `em_cjzc`。  
3. `news_us.py` 顶部仍写“两源单 job”，但注册表当前默认启用 Fed、SEC 和 8 个媒体源，共 10 个。  
4. `skills/news-opensearch/SKILL.md` 描述“四通道”，而实际 channel 已扩展到政策、媒体、研报、美国/国际等。  
5. skill 中 BM25 示例包含 `body`，实际 `news_search.py` 的 BM25 不查 `body`。  
6. `config.example.yaml` 的 `news.watch_stocks`、`news.rss_sources` 标注为二期，但当前代码没有消费这两个配置。  
7. `README.md` 引用了多个 `docs/...` 新闻设计和部署文档，但当前仓库文件清单中没有 `docs` 目录。  
8. `README.md` 的 pipeline 数量、新闻 job 数量等描述与当前配置/文件数有漂移，不能作为运行现状的唯一事实源。

## 十一、最终判断

当前新闻系统的强项是：**采集工程、数据耐久性、幂等、容错、OpenSearch 与语义检索基础。**

当前新闻系统的核心短板是：**覆盖不完整、股票关系浅、行业链路未接、没有产业链、没有重要度、没有情绪、没有事件聚类、没有面向两个目标的业务编排。**

因此：

- 目标一“全市场新闻雷达”：现有系统可作为采集和检索底座，但雷达智能层尚未建立；
- 目标二“候选股新闻深挖”：已经具备按代码查直接新闻/公告的基础，但自动扩展和综合研判层尚未建立；
- 后续应在现有底座上二次开发，不建议推倒重来。

